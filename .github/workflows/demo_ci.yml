name: Print Text on Folder Change 
on:
  push:
    paths:
      - "scripts/**"   
jobs:
  print-text-job:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Print Messages
        run: |
          echo "✅ Changes detected in scripts folder!"
          echo "🚀 Triggered on PUSH event"
          echo "🧾 Printing CICD Logs Done"
